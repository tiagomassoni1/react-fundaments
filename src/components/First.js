import React from "react";

function Primeiro () {
    const msg = 'Seja Bem-vindo(a)!'
    return (
        <div> 
            <h2>First Component</h2>
            <p>{msg}</p>
        </div>
    )
};

export default Primeiro;